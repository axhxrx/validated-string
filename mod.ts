if (import.meta.main) {
  console.log(`This is the @axhxrx/validated-string module. It doesn't have any CLI functionality, though. It exists only to help build programs that use runtime-validate strings with some modicum of build-time type-safety.`);
}
